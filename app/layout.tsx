import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Digilink IT Solutions - Connecting You to the Digital World",
  description:
    "Professional IT consulting services for individuals and companies. Expert solutions in digital transformation, AI integration, and technology consulting.",
  generator: "v0.app",
  keywords: "IT consulting, digital transformation, AI solutions, technology services, Digilink",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
